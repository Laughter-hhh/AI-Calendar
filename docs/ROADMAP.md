# AI Calendar 路线图：待拓展功能与待确认项

> 本文档记录**下一步要做什么**（有明确技术方案）和**需要用户/产品方拍板的事**。新 AI 智能体接手时，从这里挑选下一个任务，并在完成后更新本文档。

## A. 待确认项（需要用户决定，阻塞后续开发）

| # | 事项 | 影响 | 现状 |
|---|---|---|---|
| 1 | **域名 + ICP 备案**：是否购买域名并走备案（约 1~2 周） | 决定能否用域名访问、上 HTTPS（App 正式打包的前提） | 当前用 IP+端口 |
| 2 | **真实 AI Key**：是否配置 `OPENAI_API_KEY` | 解析能力从"规则版"升级为"模型版" | 未配置，本地解析器兜底 |
| 3 | **重复事件系列编辑语义**：修改/删除"这一个"还是"整个系列" | 决定重复事件 UI 交互设计 | 当前只支持单事件编辑 |
| 4 | **App 方向**：WebView 壳 vs 原生界面；是否上架应用商店 | 决定 mobile/ 的投入方向 | WebView 壳已可用 |
| 5 | **数据迁移路线**：SQLite → PostgreSQL 的时机（V3 多用户时） | 决定多用户/云同步方案 | SQLite 单文件，够小范围用 |
| 6 | **语音识别服务**：浏览器 Web Speech API（免费但依赖浏览器）vs 付费语音服务 | 语音体验稳定性 | 浏览器内置实现，Chrome/Edge 可用 |
| 7 | **注册开放策略**：任何人拿到链接都可注册，还是需要邀请/白名单 | 测试阶段的数据与账号控制 | 当前为完全开放注册 |

## B. 待拓展功能（技术方案已明确，按优先级排序）

### B1. 重复事件：系列编辑（下一个大项）

- 现状：v2.0.0 已支持 daily/weekly/monthly 的查询期展开和"仅删除本日"；v2.1.0 支持自然语言修改/删除（整个系列）
- 待做：
  - 事件卡片显示"这是重复系列"标识 + 「编辑此日 / 编辑整个系列」两个入口
  - 系列编辑：修改 base 事件（标题/时间/规则），历史已过去的日子不回写
  - 系列删除：删整个系列（按 base id 删除）或仅删当日
- 需要按待确认项 #3 拍板语义

### B1.5 语音识别（部分完成）

- 现状（v3.7.0）：桌面浏览器语音 + 手机录音上传识别（需 OPENAI_API_KEY）
- 现状补充：iPhone 可用系统键盘"听写"（苹果语音引擎）直接输入；iOS 14.5+ 浏览器语音可用
- 待做：不依赖第三方 Key 的免费语音方案评估；识别后自动提交；**iOS 原生 App Intents（"Hey Siri 添加日程"）**——依赖原生 App 路线与苹果审核政策

### B4.5 离线能力（部分完成）

- 现状（v3.10.0）：日程数据本地缓存、断网读取（页面已打开时）+ 离线提示
- 待做：完整离线壳（HTTPS + Service Worker，冷启动可用）；离线写入队列（断网新增/修改，联网后同步）——后者依赖待确认项 #5 数据迁移决策

### B9. 成本与商业化（决策参考）

- 低价 AI Key / 免费 AI / 融资时机 / 成本决策原则：见 [docs/COST-AND-FUNDING.md](COST-AND-FUNDING.md)
- 服务器配置模板：`deploy/config.env.example`（硅基流动/智谱/DeepSeek/通义一键切换）
- AI 与语音配置实战（选型/验证/安全）：见 [docs/AI-CONFIG-HANDBOOK.md](AI-CONFIG-HANDBOOK.md)

### B2. 周视图 / 月视图

- 现状：单日 / 未来 7 天 / **月视图**（v3.0.0 / v3.1.0）；区间查询已就绪
- 待做：月视图内直接创建/编辑（点击空白格创建）；拖动调整时间

### B3. AI 能力增强

- 追问记忆：当前追问只带一次上下文，可升级为会话内多轮记忆（产品文档 MVP 不存聊天历史，V2 可放开）
- 批量意图：一次输入多件事（"下午开会，晚上健身"）
- 冲突检测：新建日程与已有日程时间重叠时提示

### B3.5 日历互通与展示增强（参照主流日历应用）

- 现状（v3.1.0 / v3.2.0）：ICS 导出/导入、搜索、事件详情/备注、增强编辑、今日下一项、事件颜色分类
- 待做：农历显示；暗色模式；日历订阅（URL 只读日历）；导入去重；ICS 时区/跨天事件支持

### B4. 数据安全与运维

- 定时自动备份数据库到本地/对象存储（备份脚本已就绪：`scripts/backup.sh`，待服务器上配置 cron）
- 服务器监控：uptime/磁盘/进程自动告警（可接阿里云监控，免费）
- 数据库健康检查并入 `/api/health`（v3.0.0 已加 DB 探测 ✅）

### B5. PWA（需要先有 HTTPS）

- 依赖待确认项 #1（HTTPS）。之后补 manifest + service worker + 图标即可让网页"可安装为应用"

### B6. 用户反馈系统（V3）

- 产品文档 V3 内容：站内反馈入口 + 反馈存储（复用 events 表思路或新建 feedback 表）

### B7. 多用户与云端同步（V3）

- 当前为单库多用户（按 user_id 隔离），架构上已具备；云端同步=换 PostgreSQL + 跨设备数据合并策略

### B7.5 共享日历（部分完成）

- 现状（v3.6.0）：整个日历只读共享（按邮箱共享/撤销、共享视图带来源标注、写权限隔离）
- 待做：按分类/单个日程共享；可编辑权限；共享链接邀请；共享日程的"仅导出/不导出"设置

### B8. 应用分发与商店（部分完成）

- 现状（v3.3.0 / v3.4.0 / v3.5.1）：网页直下 APK；EAS 构建配置；下载页与下载接口；**iOS 构建配置就绪**（ATS、TestFlight 流程）；**iPhone"添加到主屏幕"PWA-lite**（图标 + manifest）；上架成本与更新策略文档
- 待做：正式图标/启动图；隐私政策页；注销账号入口；Google Play 与国内商店上架（依赖软著、HTTPS 等）；iOS 真机构建（依赖 Apple 开发者账号 $99/年，见 APP-STORE.md）

## C. 已完成功能的历史（详见 docs/versions/）

- v1.0.0：注册登录、自然语言建日程、连续 N 天、追问、语音、按日期查看、编辑删除、CI 部署、手机 App 壳
- v2.0.0：完整重复事件（每天/每周/每月 + 截止日期 + 查询期展开 + 仅本日删除）
- v2.1.0：AI 修改/删除日程（"把 X 改到 Y" / "删除 X"）
- v3.0.0：未来 7 天概览、区间查询、备份脚本、API 文档、健康检查含数据库探测
- v3.1.0：月视图、事件详情与备注、增强编辑、搜索、ICS 导出、今日下一项提示
- v3.2.0：ICS 导入、事件颜色分类
- v3.3.0：安卓独立 APK（绕过 Expo Go）+ 网页直下分发 + 商店上架成本/更新策略文档
- v3.4.0：iOS 构建配置与分发流程就绪（ATS、TestFlight 文档、商店策略）
- v3.5.0：勾选完成事项（含 AI 标记完成、完成状态展示）
- v3.5.1：iPhone"添加到主屏幕"PWA-lite（图标/清单）+ 下载页 iOS 说明
- v3.6.0：共享日历（只读）——共享/撤销、共享视图、权限控制
- v3.7.0：语音识别（手机录音上传）、自动更新提示、区间查询性能优化、定时/待办分组
- v3.7.1：AI 服务独立配置文件（服务器直配 Key 即可启用手机语音）
- v3.8.0：手机端性能重构（前端即时切换、懒加载）+ 移动界面紧凑化
- v3.9.0：网页/手机双排版（响应式断点，桌面宽松版 + 移动紧凑版）
- v3.10.0：离线缓存（本地读取）、解析修复（点半/标题）、混合识别模式
- v4.0.0：双界面（时间安排时间轴 + 单日事项）、无时间待办、界面优化、测试流
- v4.1.0：主界面极简（⋯ 更多菜单）、明亮配色
- v4.2.0：待办自动识别、项目 skill（ai-calendar-dev）、布局改动教程
- v4.3.0：截止日期自动提醒（7/3/1 天 + 当天）
- v4.3.4：截止语句强制本地规则 + 常用语句测试套件
