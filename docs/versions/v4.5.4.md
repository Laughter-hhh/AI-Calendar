# v4.5.4（2026-09-01）— 移动端加载与本周多日期解析

- 修复“本周三和周四晚上九点……”被识别为每周重复或标题残留的问题，生成当前周多个一次性日程。
- AI 标准化增加“本周不等于每周”的约束，并由服务层对有限日期语句强制使用原句确定性解析。
- 笔记页使用服务端初始数据，移除挂载后的重复列表请求，并增加路由加载骨架。
- 离线同步延迟到首屏后执行，网络请求增加超时和失败重试；Service Worker 导航超时后显示离线页。
- 删除 5 个未引用的 Next.js 默认模板静态资源。
- 新增本周多日期、笔记加载和离线超时回归测试。
