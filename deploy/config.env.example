# AI 服务配置示例
# 使用方法：复制本文件到服务器 /opt/ai-calendar/config.env，填入密钥后 pm2 restart ai-calendar
# 本项目兼容任何 OpenAI 兼容接口：改 OPENAI_BASE_URL / OPENAI_MODEL / OPENAI_API_KEY 三行即可切换平台

# —— 方案 1：硅基流动（国内直连，注册送额度，有免费模型）——
# OPENAI_BASE_URL=https://api.siliconflow.cn/v1
# OPENAI_MODEL=Qwen/Qwen2.5-7B-Instruct   # 或平台上的 GLM-4-Flash 等免费模型（以平台模型列表为准）
# OPENAI_API_KEY=sk-你的硅基流动密钥

# —— 方案 2：智谱（GLM-4-Flash 免费档）——
# OPENAI_BASE_URL=https://open.bigmodel.cn/api/paas/v4
# OPENAI_MODEL=glm-4-flash
# OPENAI_API_KEY=你的智谱密钥

# —— 方案 3：DeepSeek（低价标杆）——
# OPENAI_BASE_URL=https://api.deepseek.com/v1
# OPENAI_MODEL=deepseek-chat
# OPENAI_API_KEY=你的DeepSeek密钥

# —— 方案 4：阿里云百炼（通义千问，新用户有免费额度）——
# OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# OPENAI_MODEL=qwen-turbo
# OPENAI_API_KEY=你的百炼密钥

# 语音识别（手机端录音上传用）：需要平台支持 /audio/transcriptions（如 OpenAI 官方 Whisper）
# OPENAI_STT_MODEL=whisper-1
# 若平台不支持语音接口，手机语音可改用系统键盘"听写"（免费）
