# v4.5.5

发布日期：2026-09-01

## 修复

- 修复旧 SQLite 数据库迁移时使用非字面量默认值导致迁移中断的问题。
- 迁移失败不再缓存半初始化数据库连接，健康检查会返回失败状态并阻止错误部署。
- 新建和导入事件显式写入 `updated_at`，兼容迁移后的旧数据库。
- 修复“本周三和周四晚上九点……”解析成功但确认保存返回 500 的问题。

## 测试

- 使用缺少 `updated_at`、`series_id` 的旧事件表启动生产构建，迁移、注册、解析、保存两条日程和删除全部通过。
- 冒烟测试新增本周多日期解析、批量保存和查询回归。
